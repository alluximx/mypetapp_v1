import React from 'react';
import ContentView from '../../layouts/auth/recovery-key';

export const RecoveryKeyScreen = ({navigation}): React.ReactElement => (
  <ContentView navigation={navigation} />
);
