import React from 'react';
import ContentView from '../../../layouts/pets/add/sex-and-age';

export const SexAndAgeScreen = ({navigation, route}): React.ReactElement => {
  return <ContentView navigation={navigation} route={route} />;
};
