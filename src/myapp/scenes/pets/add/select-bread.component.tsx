import React from 'react';
import ContentView from '../../../layouts/pets/add/select-bread';

export const SelectBreadScreen = ({navigation}) : React.ReactElement => {
    return <ContentView navigation={navigation} />;
} 