import React from 'react';
import ContentView from '../../../layouts/pets/add/select-breed';

export const SelectBreedScreen = ({navigation, route}): React.ReactElement => (
  <ContentView navigation={navigation} route={route} />
);
