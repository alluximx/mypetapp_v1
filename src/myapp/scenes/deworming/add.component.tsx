import React from 'react';
import ContentView from '../../layouts/deworming/add';

export const AddDewormingScreen = ({navigation, route}): React.ReactElement => {
  return <ContentView navigation={navigation} route={route} />;
};
