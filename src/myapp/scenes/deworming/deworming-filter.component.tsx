import React from 'react';
import ContentView from '../../layouts/deworming/filter/index';

export const DewormingFilterScreen = ({
  navigation,
  route,
}): React.ReactElement => <ContentView navigation={navigation} route={route} />;
