import React from 'react';
import ContentView from '../../layouts/deworming/history';

export const DewormingHistoryScreen = ({
  navigation,
  route,
}): React.ReactElement => {
  return <ContentView navigation={navigation} route={route} />;
};
