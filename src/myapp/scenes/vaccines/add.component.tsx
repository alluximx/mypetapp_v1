import React from 'react';
import ContentView from '../../layouts/vaccines/add/index';

export const AddVaccineScreen = ({navigation}): React.ReactElement => (
  <ContentView navigation={navigation} />
);
