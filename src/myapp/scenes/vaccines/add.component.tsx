import React from 'react';
import ContentView from '../../layouts/vaccines/add/index';

export const AddVaccineScreen = ({navigation, route}): React.ReactElement => (
  <ContentView navigation={navigation} route={route} />
);
