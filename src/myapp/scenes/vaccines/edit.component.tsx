import React from 'react';
import ContentView from '../../layouts/vaccines/edit/index';

export const EditVaccineScreen = ({navigation}): React.ReactElement => (
  <ContentView navigation={navigation} />
);
