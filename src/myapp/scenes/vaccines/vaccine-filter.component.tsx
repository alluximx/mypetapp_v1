import React from 'react';
import ContentView from '../../layouts/vaccines/filter/index';

export const VaccineFilterScreen = ({
  navigation,
  route,
}): React.ReactElement => <ContentView navigation={navigation} route={route} />;
