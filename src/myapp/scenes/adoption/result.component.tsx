import React from 'react';
import ContentView from '../../layouts/adoption/result/index';
export const ResultScreen = ({navigation, route}): React.ReactElement => {
  return <ContentView navigation={navigation} route={route} />;
};
export default ResultScreen;
