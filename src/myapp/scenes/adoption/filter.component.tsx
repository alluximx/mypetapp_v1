import React from 'react';
import ContentView from '../../layouts/adoption/advanceFilter/index';
export const FilterScreen = ({navigation, route}): React.ReactElement => {
  return <ContentView navigation={navigation} route={route} />;
};
export default FilterScreen;
