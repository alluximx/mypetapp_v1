import React from 'react';
import ContentView from '../../layouts/adoption/filter/index';

export const AdoptionScreen = ({navigation}): React.ReactElement => {
  return <ContentView navigation={navigation} />;
};
