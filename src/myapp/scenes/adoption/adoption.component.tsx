import React from 'react';
import ContentView from '../../layouts/adoption/filter/index';
export const AdoptionScreen = ({navigation, route}): React.ReactElement => {
  return <ContentView navigation={navigation} route={route} />;
};
