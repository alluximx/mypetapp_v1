import React from 'react';
import ContentView from '../../layouts/adoption/request/index';
export const RequestScreen = ({navigation, route}): React.ReactElement => {
  return <ContentView navigation={navigation} route={route} />;
};
export default RequestScreen;
