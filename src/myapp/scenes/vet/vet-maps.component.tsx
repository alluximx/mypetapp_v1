import React from 'react';
import ContentView from '../../layouts/vet/maps/index';

export const VetMapsScreen = ({navigation, route}): React.ReactElement => (
  <ContentView navigation={navigation} route={route} />
);
