import React from 'react';
import ContentView from '../../layouts/vet/dates/index';

export const VetDateScreen = ({navigation, route}): React.ReactElement => {
  return <ContentView navigation={navigation} route={route} />;
};
