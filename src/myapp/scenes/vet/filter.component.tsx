import React from 'react';
import ContentView from '../../layouts/vet/filter/index';

export const VetFilterScreen = ({navigation, route}): React.ReactElement => {
  return <ContentView navigation={navigation} route={route} />;
};
