import React from 'react';
import ContentView from '../../layouts/vet/detail/index';

export const VetDetailScreen = ({navigation, route}): React.ReactElement => (
  <ContentView navigation={navigation} route={route} />
);
