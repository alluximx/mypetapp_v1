import React from 'react';
import ContentView from '../../layouts/vet/result/index';

export const VetResultScreen = ({navigation, route}): React.ReactElement => (
  <ContentView navigation={navigation} route={route} />
);
