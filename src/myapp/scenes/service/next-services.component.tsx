import React from 'react';
import ContentView from '../../layouts/services/next-services/index';

export const NextServicesScreen = ({navigation}): React.ReactElement => {
  return <ContentView navigation={navigation} />;
};
