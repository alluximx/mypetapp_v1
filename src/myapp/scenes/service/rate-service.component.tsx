import React from 'react';
import ContentView from '../../layouts/services/rate-service/index';

export const RateServiceScreen = ({navigation}): React.ReactElement => {
  return <ContentView navigation={navigation} />;
};
