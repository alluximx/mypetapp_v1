import React from 'react';
import ContentView from '../../layouts/addresses/index';

export const AddressInfScreen = ({navigation, route}): React.ReactElement => {
  return <ContentView navigation={navigation} route={route} />;
};
