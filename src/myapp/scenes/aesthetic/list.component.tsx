import React from 'react';
import ContentView from '../../layouts/aesthetic/result/index';

export const AestheticResultScreen = ({
  navigation,
  route,
}): React.ReactElement => <ContentView navigation={navigation} route={route} />;
