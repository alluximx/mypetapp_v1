import React from 'react';
import ContentView from '../../layouts/aesthetic/detail/index';

export const AestheticDetailScreen = ({
  navigation,
  route,
}): React.ReactElement => <ContentView navigation={navigation} route={route} />;
